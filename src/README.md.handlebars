Contains the [NetSuite SuiteTalk](http://www.netsuite.com/portal/developers/resources/suitetalk-documentation.shtml) [WSDL_v{{ wsdl_year }}_{{ wsdl_major }}_{{ wsdl_minor }} files](http://www.netsuite.com/download/WSDL_v{{ wsdl_year }}_{{ wsdl_major }}_{{ wsdl_minor }}.zip) with zero dependencies.

![NetSuite SuiteTalk version](https://img.shields.io/badge/NetSuite%20SuiteTalk-v{{ wsdl_year }}.{{ wsdl_major }}-blue)

![npm version](https://badge.fury.io/js/netsuite-suitetalk-wsdl-v{{ wsdl_year }}-{{ wsdl_major }}.{{ wsdl_minor }}.svg)

![npm monthly downloads](https://img.shields.io/npm/dm/netsuite-suitetalk-wsdl-v{{ wsdl_year }}-{{ wsdl_major }}.{{ wsdl_minor }})

## Install

    npm install --save netsuite-suitetalk-wsdl-v{{ wsdl_year }}-{{ wsdl_major }}.{{ wsdl_minor }}

## Usage

```
const soap = require('soap');
const wsdl = require('netsuite-suitetalk-wsdl-v{{ wsdl_year }}-{{ wsdl_major }}.{{ wsdl_minor }}');

soap.createClient(wsdl.file, (err, client) => {
  if(err) {
    console.log(err);
    process.exit(1);
  }

  // client ...
});

var wsdlVersion = wsdl.version;
// "{{ wsdl_year }}_{{ wsdl_major }}_{{ wsdl_minor }}"

```
